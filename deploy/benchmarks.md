# OCR 效能實測記錄（Benchmarks）

> 記錄不同硬體上「單張名片端到端」的耗時，作為選硬體與擴充規劃的依據。
> 管線：`圖片 → MinerU(OCR) → extract_card(candidate → 本地 LLM 分類 → validator)`。
> LLM = 本機 Ollama `qwen3:4b`。
>
> 最後更新：2026-09-02

---

## 摘要（暖機後、單張、序列處理）

| 硬體 | OCR 平均 | LLM 平均 | **總計平均** | 範圍 | 後端 |
|---|---|---|---|---|---|
| **Apple M2 Pro / 16GB**（我的電腦） | ~10.6s | ~5.9s | **~16.6s** | 14.2–20.2s | MinerU MLX + Ollama |
| **Lightning AI L4 / 24GB**（雲端） | ~3.4s | ~1.8s | **~5.2s** | 4.7–6.1s | MinerU vLLM 0.10.2 + Ollama |
| **任意 CPU VPS + `gpt-5.6-luna`**（雲端管線） | 合併 | 合併 | **~6.0s** | 3.0–9.0s | OpenAI Responses API，effort=`low` |

> 雲端管線的 OCR 與分類是**同一次呼叫**，無法拆分計時，故合併為單一數字。
> 實測環境是 Mac 本機發請求（延遲含台灣→美國往返），VPS 部署會更快一些。
> 每張成本 **US$0.00117（NT$0.038）**，詳見 §C。

**→ L4 比 Mac 快約 3.2×**（OCR 3.2×、LLM ~3×）。OCR 一直是大頭；上 GPU 的 vLLM batching 是最大槓桿。

---

## A. Apple M2 Pro / 16GB（本機開發環境）

- 系統：Apple M2 Pro / 16GB / macOS 14.7.2
- 後端：MinerU MLX（`vlm-engine` 自動選 mlx）+ Ollama `qwen3:4b`
- 暖機（僅啟動一次）：MinerU **12.77s**、LLM **6.05s**
- 資料來源：本機 benchmark 腳本（6 張名片，含中/英/日）

| 名片 | 語言 | boxes | OCR (s) | LLM (s) | 總計 (s) |
|---|---|---|---|---|---|
| test_card | zh | 7 | 11.04 | 3.72 | 14.77 |
| test_card1 | zh | 12 | 11.31 | 5.71 | 17.02 |
| test_card2 | zh | 11 | 11.04 | 5.80 | 16.83 |
| test_card3 | zh | 9 | 9.61 | 6.77 | 16.38 |
| test_card4 | en | 8 | 8.96 | 5.28 | 14.24 |
| 275709_0 | zh | 13 | 11.83 | 8.36 | 20.19 |
| **平均** | | | **10.63** | **5.94** | **16.57** |

**觀察：**
- OCR ~10.6s，佔總時間 **~64%**——瓶頸在 OCR，不在 LLM。
- 最慢的是日文名片（275709_0，20.2s），LLM 分類也最久（8.4s），符合「非標準名片較吃分類」。

---

## B. Lightning AI L4 / 24GB（雲端 GPU）

- 平台：Lightning AI Studio，GPU = NVIDIA **L4 24GB**
- 後端：MinerU **vLLM 0.10.2**（`vlm-engine`，continuous decode）+ Ollama `qwen3:4b`
- 環境：`MINERU_BACKEND=vlm-engine`、`MINERU_VIRTUAL_VRAM_SIZE=8`（vLLM 與 Ollama 共存）
- 資料來源：Lightning 執行 log（`test_card1.png`，重複上傳）

| 量測 | 值 |
|---|---|
| MinerU OCR（log `time=`，精確） | 4.09s / 2.92s / 3.06s → 平均 **~3.4s** |
| 端到端總計（暖機後） | 6.06s / 4.70s / 4.85s → 平均 **~5.2s** |
| LLM（總計 − OCR，推估） | **~1.5–2s** |

**觀察：**
- 暖機後穩定落在 **~5 秒**，達成「5–10 秒」目標。
- OCR 從 Mac 的 ~10.6s → **~3.4s**；LLM 從 ~5.9s → **~2s**。
- 準確度：test_card1 有 6/7 欄位正確；唯一漏抓為全形括號市話 `電話（04）…`（`qwen3:4b` 分類缺口，見 README「已知限制」）。

> 註：L4 端到端總計取自 log 的「上傳→200 OK」時間戳（秒級解析度，故列為 ~），
> MinerU OCR 的 `time=` 為程式內精確計時。

---

## C. 雲端管線（`cloud/` + gpt-5.6-luna）

- 模型：`gpt-5.6-luna`，`CLOUD_REASONING_EFFORT=low`、`CLOUD_IMAGE_MAX_EDGE=2048`
- 前處理：EXIF 轉正 → 白底去 alpha → 長邊縮到 2048（本批全部原本就 <2048，故未縮）→ JPEG q88
- 資料：`test/` 全部 12 張（中／英／日），2026-09-04 實測
- 發請求端：Mac 本機（延遲含跨國往返；VPS 部署會更快）

| 名片 | 送出尺寸 | 填滿欄位 | issue | repair | in | out（含 reasoning） | 秒 | 成本 |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| 275709_0 | 1704×961 | 8/10 | 0 | 0 | 3,432 | 660 (406) | 9.03 | $0.00148 |
| 275710_0 | 1704×961 | 8/10 | 0 | 0 | 3,432 | 694 (364) | 7.19 | $0.00152 |
| 275711_0 | 1704×961 | 8/10 | 0 | 0 | 3,432 | 475 (266) | 6.03 | $0.00126 |
| 275712_0 | 1704×961 | 7/10 | 0 | 0 | 3,432 | 819 (179) | 8.15 | $0.00167 |
| 275713_0 | 1704×961 | 8/10 | 0 | 0 | 3,432 | 468 (197) | 5.63 | $0.00125 |
| 275714_0 | 1704×961 | 8/10 | 0 | 0 | 3,432 | 325 (131) | 4.57 | $0.00108 |
| 275715_0 | 961×1704 | 9/10 | 0 | 0 | 3,432 | 404 (180) | 4.63 | $0.00117 |
| test_card | 715×441 | 8/10 | 0 | 0 | 1,810 | 482 (265) | 5.40 | $0.00094 |
| test_card1 | 1078×650 | 6/10 | 0 | 0 | 2,280 | 714 (516) | 8.19 | $0.00131 |
| test_card2 | 1084×672 | 6/10 | 0 | 0 | 2,280 | 204 (0) | 2.95 | $0.00070 |
| test_card3 | 1074×632 | 8/10 | 0 | 0 | 2,240 | 442 (227) | 5.92 | $0.00098 |
| test_card4 | 697×430 | 6/10 | 0 | 0 | 1,793 | 307 (154) | 4.21 | $0.00073 |
| **平均** | | **7.5/10** | **0** | **0** | **2,886** | **500 (240)** | **5.99** | **$0.00117** |

**觀察：**

- **12/12 全部成功，validator 零 issue、零 repair。** 重用的 regex 把關這一批完全沒有攔到東西
  ——`gpt-5.6-luna` 的輸出品質遠高於 `qwen3:4b`（後者需要 repair 機制來補）。
  這也意味著 `CLOUD_MAX_REPAIR=1` 在正常情況下不會產生額外成本。
- **`test_card1.png` 的驗收基準通過。** §B 記載 GPU 管線在這張漏抓了全形括號市話
  `電話（04）…`；雲端管線抓到了，而且**表面字串原樣保留**：
  `phone='（04）2326-2888'`、`fax='（04）2326-2788'`。
  這正是 SYSTEM_PROMPT 規則 5（逐字元照抄、保留括號）與 `_snap_candidates` 一起要達成的效果。
- **速度 ~6.0s/張**，與 L4 的 5.2s 同級，但硬體成本為零。
  最慢 9.0s、最快 3.0s，差異主要來自 reasoning tokens（0–516 不等）。
- **「填滿欄位 7.5/10」不等於準確度**——名片本來就常沒有 email / website / 英文名。
  逐欄正確性需與 §A/§B 的 GPU 結果人工比對，尚未做。

### 成本

| 情境 | 每張 | 100 張/天 | 200 張/天 |
|---|---:|---:|---:|
| 本批（掃描圖，多數 <2048px 未縮） | $0.00117（NT$0.038） | **US$3.5/月** | **US$7.0/月** |
| 手機實拍推估（縮到 2048×1536，image tokens 856→3,686） | ~$0.0016（NT$0.051） | ~US$4.7/月 | ~US$9.4/月 |

> 原先計畫估的是 $0.0017/張，實測掃描圖是 $0.00117——低估了圖片尺寸的影響
> （本批多數圖原本就小於 2048，根本沒觸發縮圖）。手機實拍會接近原估值。

### 重跑方式

```bash
cp .env.cloud.example .env.cloud     # 填入 OPENAI_API_KEY
bash start.cloud.sh
curl -X POST :8100/api/cards/upload -F "file=@test/test_card1.png"
```

server log 每次呼叫都會印
`Vision extract done: model=… effort=… in= cached= out= reasoning= time=`，
可直接換算成本與延遲。

---

## 待補（Roadmap 相關）

- [ ] **L4 併發吞吐**：改 vLLM server 模式（`vlm-http-client`）後，開多併發實測單卡「張/分」
      → 驗證 `hybrid-scaling-plan.md` §1 的「單卡 batching 2–4×」。
- [ ] **RTX 3090 / 4070 Ti Super**：買卡後補自建機的單張與併發數據。
- [ ] **雲端 burst worker**（RunPod/Modal）冷啟動時間量測。
- [x] ~~**雲端管線實測**：12 張全批的 token 與成本、延遲（見 §C，2026-09-04）~~
- [ ] **雲端管線逐欄準確度**：與 §A/§B 的 GPU 結果人工比對每個欄位對錯（§C 只記了「填滿幾個」）。
- [ ] **reasoning effort A/B**：`none` / `low` / `medium` 各跑一輪 12 張（總計 <US$0.10），
      用數據決定預設值。實測 reasoning tokens 佔 output 的 0–72%，是成本的主要變數。
- [ ] **手機實拍**：12MP + EXIF 旋轉 + HEIC 的真實照片（`test/` 全是乾淨掃描，涵蓋不到）。

---

相關文件：[`hybrid-scaling-plan.md`](hybrid-scaling-plan.md)（擴充架構與成本）、[`../README.md`](../README.md)（架構總覽）

# x86_64 via Rosetta 2 (Apple Silicon Mac using /usr/local/bin/python3)
# Step 1: pip install paddlepaddle==2.6.2 paddleocr==2.6.1.3 --no-deps
# Step 2: pip install -r requirements-x86.txt
#
# NOTE: paddlepaddle and paddleocr must be installed with --no-deps (see Step 1)
# to avoid pulling in unneeded build-time packages that require system tools like swig.

# numpy must be <2.0 for paddlepaddle compatibility
numpy<2.0

# opencv: pin to 4.6.x to avoid numpy>=2 requirement from newer versions
opencv-python<=4.6.0.66
opencv-contrib-python<=4.6.0.66
opencv-python-headless<=4.6.0.66

pyclipper
shapely
lmdb
rapidfuzz
imgaug
scikit-image
tqdm
requests
pillow

fastapi
uvicorn[standard]
sqlalchemy
python-multipart
python-dotenv
opencc-python-reimplemented

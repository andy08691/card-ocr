# ARM native (Apple Silicon M1/M2/M3)
# Step 1: pip install paddlepaddle==0.0.0 -f https://www.paddlepaddle.org.cn/whl/mac/cpu/develop.html
# Step 2: pip install paddleocr==2.6.1.3 --no-deps
# Step 3: pip install -r requirements-arm.txt
#
# NOTE: paddleocr must be installed with --no-deps to avoid pulling in unneeded
# build-time packages (PyMuPDF etc.) that require system tools like swig.

# numpy must be <2.0 for paddlepaddle ARM compatibility
numpy<2.0

# opencv: pin to 4.6.x to avoid numpy>=2 requirement from newer versions
opencv-python<=4.6.0.66
opencv-contrib-python<=4.6.0.66
opencv-python-headless<=4.6.0.66

pyclipper
shapely
lmdb
rapidfuzz
imgaug
scikit-image
tqdm
requests
pillow

fastapi
uvicorn[standard]
sqlalchemy
python-multipart
python-dotenv
opencc-python-reimplemented
